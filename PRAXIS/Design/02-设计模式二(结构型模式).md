# 代理模式

概念：为其他对象提供一种代理以控制对此对象的访问

代理：一个具有与被代理对象相同接口的类，客户端只有通过 Proxy 来实现与被代理类的交互，并且在交互过程中 ，代理可以增加其它操作
    - 远程代理：隐藏一个对象在不同地址空间的事实
    - 虚拟代理：存放需要很长时间实例化的对象
    - 安全代理：控制真实对象的访问权限
    - 智能引用；当调用真实对象时，代理处理另外一些事


优点：

    - 职责清晰
    - 高拓展
    - 智能化
  
缺点：
    - 增加系统复杂度
    - 请求速度变慢

```cpp
// 主题接口
class Subject {
public:
    virtual ~Subject() {}
    virtual void request() = 0;
};

// 真实主题
class RealSubject : public Subject {
public:
    void request() override {
        std::cout << "RealSubject: handling request" << std::endl;
    }
};

// 代理类
class Proxy : public Subject {
private:
    std::unique_ptr<RealSubject> realSubject_;
    
    void checkAccess() {
        std::cout << "Proxy: checking access" << std::endl;
    }
    
    void logAccess() {
        std::cout << "Proxy: logging access" << std::endl;
    }
    
public:
    void request() override {
        checkAccess();        // 拓展新功能
        if (!realSubject_) {  // 代理真实主题
            realSubject_ = std::make_unique<RealSubject>();
        }
        realSubject_->request();
        logAccess();
    }
};

```